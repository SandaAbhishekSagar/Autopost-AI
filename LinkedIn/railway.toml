[build]
builder = "NIXPACKS"
buildCommand = "pip3 install --upgrade pip setuptools wheel && pip3 install -r requirements.txt"

[deploy]
startCommand = "python3 app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

