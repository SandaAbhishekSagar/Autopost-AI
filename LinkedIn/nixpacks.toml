[phases.setup]
nixPkgs = ["python313", "python313Packages.pip", "python313Packages.setuptools", "python313Packages.wheel"]

[phases.install]
cmds = [
  "pip3 install --user -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python3 app.py"

